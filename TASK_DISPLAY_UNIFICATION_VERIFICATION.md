# タスク一覧表示統一 - 動作検証手順

## 実装内容

### 変更したファイル
- `app/views/tasks/index.html.haml`: タスク一覧ページ

### 統一したポイント

#### 1. 共通パーシャルの使用
すべてのタスク表示で `task_card` パーシャルを使用：
- ✅ ドラフトタスク（AI抽出タスク）
- ✅ 未完了タスク（pending-tab）
- ✅ 完了済みタスク（completed-tab）

#### 2. 親要素の構造統一
すべてのタスクリストに一貫したID命名とクラス構造を適用：

```haml
# ドラフトタスク
#draft-tasks.space-y-3

# 未完了タスク
#published-tasks.space-y-3

# 完了済みタスク
#completed-tasks.space-y-3
```

#### 3. スタイルの統一
- すべてのリストに `space-y-3` クラスを適用（タスク間の統一された間隔）
- タブコンテナに共通のパディング `p-6` を適用
- カード表示形式の完全統一

## 動作検証手順

### テスト1: 初期表示の確認

1. **タスク一覧ページにアクセス**
   ```
   http://localhost:3000/tasks
   ```

2. **未完了タブ（デフォルト表示）の確認**
   - ページを開いたとき、未完了タブが選択されている
   - タスクがカード形式で表示されている
   - 各カードに以下が含まれる：
     - ✅ カテゴリアイコン
     - ✅ タスクタイトル
     - ✅ 説明（description）があれば表示
     - ✅ カテゴリ、嫌い度、作成日時の情報
     - ✅ 期限表示（設定されている場合）
     - ✅ アクションボタン（編集、LINE通知、完了、非表示）

### テスト2: タブ切り替えでの表示確認

1. **完了済みタブに切り替え**
   - 「完了済み」タブをクリック
   - タスク表示が切り替わることを確認
   - カード形式が保持されていることを確認
   - 完了済みタスクには以下が表示される：
     - ✅ チェックマーク
     - ✅ タスクタイトル
     - ✅ 説明（description）があれば表示
     - ✅ 完了日時と獲得した強靭さポイント

2. **再度未完了タブに切り替え**
   - 「未完了」タブをクリック
   - タスク表示が元に戻ることを確認
   - レイアウトとスタイルが初期表示と完全に一致することを確認

### テスト3: ドラフトタスク表示の確認

1. **AI抽出タスクがある場合**
   - ページ上部にドラフトタスクセクションが表示される
   - 紫色の背景で強調表示
   - カード形式で表示される：
     - ✅ 抽出インデックス
     - ✅ カテゴリバッジ
     - ✅ 信頼度バッジ
     - ✅ タスクタイトル
     - ✅ 説明（description）があれば表示
     - ✅ 抽出元テキスト
     - ✅ 承認/却下ボタン

2. **ドラフトタスクの承認**
   - 「✅ 承認」ボタンをクリック
   - ドラフトセクションから消える
   - 未完了タブのリストに動的に追加される
   - 追加されたタスクのスタイルが既存のタスクと完全に一致することを確認

### テスト4: レスポンシブデザインの確認

1. **デスクトップ表示（1280px以上）**
   - すべてのタスクが適切に表示される
   - ボタンが横並びで表示される

2. **タブレット表示（768px - 1279px）**
   - レイアウトが調整される
   - カード形式は維持される

3. **スマホ表示（767px以下）**
   - Chrome DevTools: Cmd+Shift+M (Mac) / Ctrl+Shift+M (Windows)
   - iPhone または Android デバイスを選択
   - タスクカードが縦に積み重なる
   - すべての情報が読みやすく表示される
   - ボタンが適切にレイアウトされる

### テスト5: ダークモードでの表示確認

1. **ダークモード切り替え**
   - システム設定でダークモードを有効化
   - またはブラウザの開発者ツールでダークモードをエミュレート

2. **すべてのタブで確認**
   - 未完了タブ
   - 完了済みタブ
   - ドラフトタスク

3. **確認ポイント**
   - 背景色が適切に変更される
   - テキストの可読性が保たれる
   - ボタンのコントラストが十分

### テスト6: タスク操作の確認

1. **新しいタスクを作成**
   - 「タスク追加」ボタンをクリック
   - フォームに入力：
     - タスク名: 「表示統一テスト」
     - カテゴリ: 任意
     - 説明: 「これはテスト用のタスクです。\n改行も含みます。」
   - 「タスクを追加」ボタンをクリック

2. **作成されたタスクの確認**
   - ダッシュボードにリダイレクトされる
   - タスク一覧に戻る
   - 新しいタスクが未完了タブに表示される
   - 表示形式が他のタスクと完全に一致することを確認

3. **タスクを完了**
   - タスクの「🎯 完了」ボタンをクリック
   - 確認ダイアログで承認
   - ページがリロードされる
   - 完了済みタブに切り替え
   - 完了したタスクが表示される
   - 完了日時が記録されている

## 期待される結果

### ✅ 成功の判断基準

1. **視覚的統一性**
   - すべてのタブで同じカードデザイン
   - 同じフォントサイズとカラー
   - 同じ間隔とパディング

2. **機能的一貫性**
   - タブ切り替えがスムーズ
   - ドラフト承認時の動的追加が正常動作
   - すべてのボタンが機能する

3. **レスポンシブ対応**
   - すべての画面サイズで適切に表示
   - タッチデバイスでも操作しやすい

4. **アクセシビリティ**
   - ダークモードでも見やすい
   - 情報が明確に伝わる

## トラブルシューティング

### 問題: タブ切り替えが動作しない

**原因**: JavaScript エラー

**対処法**:
1. ブラウザのコンソールを開く（F12 または Cmd+Option+I）
2. エラーメッセージを確認
3. ページをリロード（Cmd+R / Ctrl+R）

### 問題: ドラフト承認後にタスクが表示されない

**原因**: Turbo Stream のターゲットID不一致

**対処法**:
1. `app/controllers/tasks_controller.rb` の `approve` アクションを確認
2. `turbo_stream.prepend("published-tasks", ...)` が正しいか確認
3. `app/views/tasks/index.html.haml` で `#published-tasks` が存在するか確認

### 問題: スタイルが一部異なる

**原因**: CSS キャッシュまたは未適用のスタイル

**対処法**:
1. ブラウザのキャッシュをクリア
2. ページを強制リロード（Cmd+Shift+R / Ctrl+Shift+R）
3. `rails assets:precompile` を実行（本番環境の場合）

## 技術的詳細

### パーシャル構造

```
app/views/tasks/
├── index.html.haml           # メインのタスク一覧ページ
├── _task_card.html.haml      # 統一されたタスクカード表示
├── _task_item.html.haml      # ダッシュボード用（コンパクト表示）
└── _task.html.haml           # レガシー（未使用）
```

### ID命名規則

| セクション | ID | 用途 |
|-----------|-------|------|
| ドラフトタスクリスト | `#draft-tasks` | AI抽出タスクのコンテナ |
| 未完了タスクリスト | `#published-tasks` | 承認済み未完了タスクのコンテナ |
| 完了済みタスクリスト | `#completed-tasks` | 完了したタスクのコンテナ |
| 未完了タブ | `#pending-tab` | 未完了タブのコンテンツエリア |
| 完了済みタブ | `#completed-tab` | 完了済みタブのコンテンツエリア |

### CSS クラス

| クラス | 効果 |
|--------|------|
| `.space-y-3` | タスク間の垂直方向の間隔（0.75rem） |
| `.p-6` | パディング 1.5rem（タブコンテナ） |
| `.rounded-lg` | 角丸（カード） |
| `.shadow-sm` | 軽い影（カード） |

## まとめ

この実装により、タスク一覧ページのすべてのセクション（ドラフト、未完了、完了済み）で統一されたタスク表示が実現されました。

**主な改善点:**
- 共通パーシャル（`task_card`）の一貫した使用
- 親要素の構造とID命名の統一
- すべてのタブで同じスタイルとレイアウト
- Turbo Stream による動的更新の正常動作

ユーザーは、どのタブを表示しても、一貫した見た目と操作感を体験できます。
