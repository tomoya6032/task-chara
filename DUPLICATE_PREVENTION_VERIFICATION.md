# タスク作成時の二重送信防止機能 - 動作検証手順

## 実装内容

### 1. フロントエンド（UI）側の防止策
- **送信ボタンの無効化**: `data-disable-with="作成中..."` 属性により、送信後にボタンが自動的に無効化される
- **視覚的フィードバック**: ボタンのテキストが「タスクを追加」→「作成中...」に変更される
- **対象ファイル**: `app/views/tasks/new.html.haml` (2箇所: 別ウィンドウ版とモーダル版)

### 2. バックエンド（コントローラー）側の防止策
- **セッションベースのトークン管理**: タスクの内容（タイトル、カテゴリ、嫌い度、期限）からハッシュトークンを生成
- **5秒間の重複リクエスト拒否**: 同一トークンのリクエストが5秒以内に来た場合は拒否
- **自動復旧**: バリデーションエラー時はトークンをクリアし、再送信可能にする
- **対象ファイル**: `app/controllers/tasks_controller.rb`

### 3. データベース（モデル）側の防止策
- **重複タスクチェック**: 同じキャラクターで同じタイトルのタスクが5秒以内に作成されていないか検証
- **バリデーションエラー**: 重複検出時は「同じタスクが既に作成されています」というエラーを表示
- **対象ファイル**: `app/models/task.rb`

## 動作検証手順

### テスト1: 基本的な二重送信防止（ボタン連打）

1. **タスク作成画面を開く**
   - ダッシュボードまたはタスク一覧から「新しいタスクを追加」をクリック

2. **フォームに入力**
   - タスク名: 「テスト連打タスク」
   - カテゴリ: 任意選択
   - その他: デフォルトのまま

3. **送信ボタンを素早く2回連打**
   - 1回目のクリック直後、ボタンが「作成中...」に変わり、無効化されることを確認
   - 2回目のクリックは受け付けられないことを確認

4. **結果確認**
   - タスク一覧を確認し、「テスト連打タスク」が**1つだけ**作成されていることを確認
   - 2つ以上作成されていないことを確認

### テスト2: JavaScriptによる強制的な二重送信（開発者ツール）

1. **タスク作成画面を開く**
   - Chrome/Safari開発者ツールを開く（F12 または Cmd+Option+I）

2. **フォームに入力**
   - タスク名: 「テスト強制送信」
   - カテゴリ: 任意

3. **開発者ツールのコンソールで以下を実行**
   ```javascript
   // フォームを2回連続で送信
   const form = document.querySelector('form');
   form.requestSubmit();
   setTimeout(() => form.requestSubmit(), 100);
   ```

4. **結果確認**
   - コントローラーログに「⚠️ Duplicate task creation attempt detected」が表示されることを確認
   - タスクが**1つだけ**作成されていることを確認

### テスト3: モデルレベルの重複防止

1. **Railsコンソールを開く**
   ```bash
   rails console
   ```

2. **同じタスクを5秒以内に2回作成を試行**
   ```ruby
   character = Character.first
   
   # 1回目の作成（成功するはず）
   task1 = character.tasks.create!(
     title: "テスト重複防止",
     category: "work",
     dislike_level: 5
   )
   puts "1回目: #{task1.persisted? ? '成功' : '失敗'}"
   
   # 2回目の作成（5秒以内、失敗するはず）
   task2 = character.tasks.create(
     title: "テスト重複防止",
     category: "work",
     dislike_level: 5
   )
   puts "2回目: #{task2.persisted? ? '成功（問題）' : '失敗（正常）'}"
   puts "エラー: #{task2.errors.full_messages.join(', ')}"
   ```

3. **期待される結果**
   - 1回目: 成功
   - 2回目: 失敗（バリデーションエラー）
   - エラーメッセージ: 「同じタスクが既に作成されています。しばらく待ってから再度お試しください。」

4. **5秒後に再試行**
   ```ruby
   sleep 6  # 6秒待機
   
   # 3回目の作成（5秒以上経過後、成功するはず）
   task3 = character.tasks.create(
     title: "テスト重複防止",
     category: "work",
     dislike_level: 5
   )
   puts "3回目: #{task3.persisted? ? '成功（正常）' : '失敗（問題）'}"
   ```

5. **期待される結果**
   - 3回目: 成功（5秒以上経過しているため）

### テスト4: バリデーションエラー時の再送信可能性

1. **タスク作成画面を開く**
   - タスク名を**空白**のままにする（バリデーションエラーを発生させる）

2. **送信ボタンをクリック**
   - 「タスク名を入力してください」というエラーが表示される
   - ボタンが再び有効化され、再送信可能になることを確認

3. **正しい情報を入力して再送信**
   - タスク名: 「テスト再送信」
   - その他: 適切に入力

4. **結果確認**
   - タスクが正常に作成されることを確認

### テスト5: スマホ（タッチデバイス）での連打テスト

1. **スマホまたはDevToolsのレスポンシブモードで表示**
   - Chrome DevTools: Cmd+Shift+M (Mac) / Ctrl+Shift+M (Windows)
   - デバイスを「iPhone」または「Android」に設定

2. **タスク作成フォームを開く**
   - タスク名: 「SPテスト連打」
   - カテゴリ: 任意

3. **送信ボタンを素早く複数回タップ**
   - 指またはマウスで連続タップを試行

4. **結果確認**
   - ボタンが即座に無効化されることを確認
   - タスクが**1つだけ**作成されることを確認

## トラブルシューティング

### ケース1: ボタンが無効化されない
- **原因**: JavaScriptが正しく読み込まれていない
- **対処**: 
  - ページを再読み込み（Cmd+R / Ctrl+R）
  - ブラウザキャッシュをクリア

### ケース2: 5秒後も再送信できない
- **原因**: セッションにトークンが残っている
- **対処**:
  - ブラウザのセッションをクリア（新しいプライベートウィンドウで試す）
  - `rails console` で `Rails.cache.clear` 実行

### ケース3: エラーメッセージが表示されない
- **原因**: ログレベルが適切でない
- **対処**:
  - `config/environments/development.rb` で `config.log_level = :info` を確認
  - `tail -f log/development.log` でログをリアルタイム監視

## ログの確認方法

### コントローラーログ
```bash
tail -f log/development.log | grep "Duplicate task creation"
```

期待される出力:
```
⚠️ Duplicate task creation attempt detected: [トークンハッシュ]
```

### モデルバリデーションログ
```bash
tail -f log/development.log | grep "Duplicate task creation blocked"
```

期待される出力:
```
⚠️ Duplicate task creation blocked: character_id=1, title=テスト連打タスク
```

## セキュリティと制限事項

1. **5秒間の制限**: 正当なユーザーが5秒以内に同じタスクを作成することは通常ないため、この制限は適切
2. **セッションベース**: ユーザーごとに独立したトークン管理のため、他のユーザーへの影響なし
3. **ハッシュ化**: タスク情報をSHA256でハッシュ化し、セキュアに管理
4. **自動クリーンアップ**: バリデーションエラー時は自動的にトークンをクリアし、再試行を許可

## まとめ

この実装により、以下の3層で二重送信を防止:
1. **UI層**: ボタン無効化による物理的な防止
2. **コントローラー層**: セッショントークンによる論理的な防止
3. **モデル層**: データベースクエリによる最終的な防止

通常のユーザー操作では、UI層の防止で十分に機能します。悪意のあるリクエストや通信エラーによる重複の場合でも、コントローラー層とモデル層で確実にブロックされます。
